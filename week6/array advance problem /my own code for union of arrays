// Online C++ compiler to run C++ program online
#include <iostream>
#include <bits/stdc++.h>
using namespace std ;

int main(){
    int n = 5 ;
    int a[n] = {1,2,2,3,4} ;
    int b[n] = {3,3,5,6,7} ;
    set <int> s ;
    for (int i=0 ; i<n ; i++){
        s.insert(a[i]);
        s.insert(b[i]);
    }
    for (auto x : s)
        cout << x << " ";
    
    
    
    return 0;
}
