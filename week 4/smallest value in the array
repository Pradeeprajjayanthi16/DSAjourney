#include <iostream>
using namespace std ;
int main() {
    int n = 5;
    int arr[10] ;
    for(int j=0;j<n;j++){
        cin >> arr[j];
    }
    int smallest = arr[0];
    for(int i=0;i<n;i++){
    if(arr[i]<smallest)
    {
        smallest = arr[i];
    }
}
cout << smallest;
    return 0;
}
